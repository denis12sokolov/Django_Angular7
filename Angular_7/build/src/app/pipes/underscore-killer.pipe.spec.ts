import { UnderscoreKillerPipe } from './underscore-killer.pipe';

describe('UnderscoreKillerPipe', () => {
  it('create an instance', () => {
    const pipe = new UnderscoreKillerPipe();
    expect(pipe).toBeTruthy();
  });
});
